// 权限

import types from '../types';

const _state = {

};

const mutations = {
    [types.auth.SET_PRIVS] () {

    }
};

const actions = {
    [types.auth.FETCH_PRIVS] () {

    }
};

export default {
    state: _state,
    mutations,
    actions
};